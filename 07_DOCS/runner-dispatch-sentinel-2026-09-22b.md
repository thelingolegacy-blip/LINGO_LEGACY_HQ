# Runner Dispatch Sentinel

Diagnostic-only artifact. No production activation, DNS mutation, deployment, or LKG promotion is authorized by this file.

Purpose: trigger the repository's existing `pull_request` runner-route probe from a clean branch based on `main`.
